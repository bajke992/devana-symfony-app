{% extends create == 'true' ? 'BookBundle:Default:create.html.twig' : 'BookBundle:Default:update.html.twig' %}

{% if create == 'false' %}
    {% block book_id %}{{ book.id }}{% endblock %}
{% endif %}

{% block form %}

    {{ form_start(form) }}

        {{ form_errors(form) }}

        {{ form_row(form.title) }}
        {{ form_errors(form.title) }}

        {{ form_row(form.description) }}
        {{ form_errors(form.description) }}

        {{ form_row(form.save) }}

    {{ form_end(form) }}

{% endblock %}